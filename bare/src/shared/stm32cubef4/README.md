This directory contains symlinks to the used parts of ST's STM32F4Cube (firmware
package with source code, especially device drivers and BSPs). These links are
both used by `Makefiles` and for code tagging.
